'use strict';
const router = require('express').Router();
// TODO: Codex — implement credit pull routes per MVP brief
router.get('/', (_req, res) => res.json({ message: 'Credit route — coming soon' }));
module.exports = router;
